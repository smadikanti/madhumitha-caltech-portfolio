apiVersion: v1
kind: Service
metadata:
  name: ci-cd-toolkit
  labels:
    app.kubernetes.io/name: ci-cd-toolkit
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: ci-cd-toolkit
